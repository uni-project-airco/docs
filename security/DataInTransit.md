+ Encrypted Communication:
All information sent from the IoT device to backend to mobile application is sent over TLS encrypted channels to avoid interception when data is in transit.

+ Single Communication Path:
At present, one channel of communication exists to communicate an expected amount of data. This avoids unnecessary configurations, however, everything that is sent as information is sent over a singular, regulated path. This makes it easier to monitor things and avoid accidental exposure on multiple paths.